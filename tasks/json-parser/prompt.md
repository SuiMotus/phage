write a recursive descent JSON parser in python. no `json` module.

function `parse_json(s)` takes a string and returns the parsed python object.

support: strings (with escapes), numbers (int and float), booleans, null, arrays, objects.

raise `ValueError` on invalid input.

save to `solution.py`.
