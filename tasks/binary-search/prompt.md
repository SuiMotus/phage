write a python function `binary_search(arr, target)` that returns the index of `target` in sorted list `arr`, or -1 if not found.

handle edge cases:
- empty array
- single element
- target smaller than all elements
- target larger than all elements
- duplicate elements (return leftmost index)

save to `solution.py`.
