You have a file called `sales.csv` with columns: date, region, product, amount.
Write a Python script called `solve.py` that reads the CSV and prints the total
amount for rows where region is "west" and amount is greater than 100.
Print only the number, no labels.
